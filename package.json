{
  "scripts": {
    "dev": "run-p backend frontend",
    "backend": "npm run --prefix backend/ dev",
    "frontend": "npm run --prefix frontend/ start"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
